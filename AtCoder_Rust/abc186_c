// use std::cmp::max;
// use std::cmp::min;

fn main() {
    proconio::input! {
        n: usize,
    }
    let mut count: Vec<usize> = vec![1; n];

    for num in 0..n {
        println!("{}", num);
    }
}
